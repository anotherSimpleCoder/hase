pipeline {
    agent none

    stages {
        stage('Test') {
            agent {
                docker {
                    image 'maven:3.9.9-amazoncoretto-21'
                    args '-u root'
                }
            }

            steps {
                dir('backend') {
                    sh 'mvn clean test'
                }
            }
        }
    }
}